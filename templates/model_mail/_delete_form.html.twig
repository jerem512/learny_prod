<form method="post" action="{{ path('app_model_mail_delete', {'id': model_mail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ model_mail.id) }}">
    <button class="btn btn-outline-danger">Supprimer</button>
</form>
